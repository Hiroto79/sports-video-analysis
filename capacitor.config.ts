import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.sportsvideoanalysis',
  appName: 'SportsVideoAnalysis',
  webDir: 'dist'
};

export default config;
